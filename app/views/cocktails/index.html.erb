<div class="container ">
  <div class="wrapper">
    <div class="row d-flex">
        <% @cocktails.each do |cocktail| %>
          <div class="card-deck m-2  align-items-center">
          <%= render "card", cocktail: cocktail %>
          </div>
        <% end %>
    </div>
  </div>
</div>
