<div class="container">
  <div class="wrapper">
    <div class="row">
      <div class="col-12">
        <%= simple_form_for [@cocktail, @dose] do |f| %>
          <%= f.input :description %>
          <%= f.association :ingredient %>
          <%= f.submit "add a Dose", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>

